import styled from 'styled-components/native';

const ContainerSectionTitle = styled.View`
  flex-direction: row;

  align-items: center;
`;

const ContainerIcon = styled.View`
  margin-right: 8px;
`;

export { ContainerSectionTitle, ContainerIcon };
